# PCDA Coach Portal — Fixed Starter (Next.js + Supabase)

- Path alias configured: `@/*` -> project root
- `lib/store.ts` stub included
- Email magic-link login (Supabase)
- Protected dashboard listing published updates
- `supabase.sql` & `seed.sql` included

## Quick Start
```bash
npm install
cp .env.example .env.local
npm run dev
```
